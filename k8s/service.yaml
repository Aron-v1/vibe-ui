apiVersion: v1
kind: Service
metadata:
  labels:
    app: govuk-reuse-library
    component: frontend
  name: govuk-reuse-library
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: govuk-reuse-library
  sessionAffinity: None
  type: ClusterIP

